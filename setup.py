#!/usr/bin/env python

from setuptools import setup

setup(
    name='waymo2bag',
    version='1.0',
    description='Convert Waymo dataset to ROS bag file the easy way!',
    author='Yusuke Muramatsu',
    url='',
    # entry_points={
    #     'console_scripts': ['waymo2bag=waymo2bag.__main__:main'],
    # },
    # install_requires=[]
)
